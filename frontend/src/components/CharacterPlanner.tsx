export function CharacterPlanner() {
  return (
    <section className="rounded-md border border-slate-200 bg-white p-4 shadow-panel">
      <h2 className="text-lg font-semibold text-slate-950">Character Planner</h2>
      <p className="mt-2 text-sm text-slate-600">
        MVP planning uses saved diagnoses. Build one main DPS first, then one support, then improve echo quality.
      </p>
    </section>
  );
}
