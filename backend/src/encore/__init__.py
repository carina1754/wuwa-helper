"""encore.moe API client + refresh layer for Wuthering Waves game data.

encore.moe (api-v2.encore.moe) exposes clean structured JSON per entity — the
canonical source for complete numeric data (per-level stat growth curves,
ascension, skills, resonance chains, weapon curves, echo sonata sets). This
replaces the fragile wuthering.gg chunk scraping for the ``wuwa_*`` tables.
"""
