.env
.env.local
.env.*.local
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo
*.db
*.sqlite
*.sqlite3
uploads/
certificates/
*.log
caddy/*.exe
caddy/certs/
DB/
backend/.env
media/
backend/media/
.tokensave
backend/.encore_cache/
backend/scratchpad/
# Game datamine (user-maintained per patch, ~1GB — reference data, never commit)
/WutheringWaves_Data/
/WutheringWaves_Data-*/
/WutheringWaves_Data-*.zip

# Standalone build artifacts (regenerated from frontend/ by build step)
backend/static/
# PyInstaller outputs (regenerated by build_exe.ps1)
backend/build/
backend/dist/
# Local-file history/logs (per-user runtime data, not source)
backend/data/local/
# Reference clone (bytonylee/free-router) — not part of this repo
/free-router/
# Ad-hoc screenshots dropped in repo root
/*.png

# local-only
.claude/
docs/
